{
  "name": "qefro-landing-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build:motion": "esbuild src/motion/index.js --bundle --format=esm --minify --target=es2018 --outfile=assets/js/qefro-motion.js",
    "build": "npm run build:motion && python3 generate.py"
  },
  "dependencies": {
    "motion": "^12.23.12"
  },
  "devDependencies": {
    "esbuild": "^0.25.8"
  }
}
